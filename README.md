# SortZip

> 文件分类 → 顺序重命名 → 分组加密压缩，一站式批处理工具。

仅支持结合 **Bandizip** 使用，基于其命令行实现自动分卷、密码保护和二次打包归档。

---

## 功能流程

1. **分类** —— 按扩展名将源文件夹中的文件自动归类至子目录（支持自定义分类名，可勾选启用）
2. **重命名** —— 每个目录内按文件名或修改时间排序，顺序重命名为 `1.ext`、`2.ext`…
3. **分组压缩** —— 每 N 个文件为一组，使用 Bandizip 带密码压缩：
   - 自动检测分卷大小（<4GB 时分半，≥4GB 时每卷 4GB）
   - 支持手动指定分卷大小
4. **二次打包**（可选）—— 将分卷文件再次压缩为一个 `.zipp` 文件

执行过程支持 **实时进度条**、**取消操作**，完成后弹出 **统计报告**。

---

## 快速开始

### 环境要求

- Windows 操作系统
- [Bandizip](https://www.bandizip.com/) 已安装并加入 PATH（或配置完整路径）

### 使用方法

直接运行 `dist/SortZip.exe` 即可启动图形界面。

---

## 配置说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `src` | `str` | — | 源文件夹路径（支持拖入，自动记忆） |
| `dest` | `str` | — | 目标根目录（支持拖入，自动记忆） |
| `group_size` | `int` | `1` | 每组文件数（自动记忆） |
| `sort_by` | `str` | `name` | 排序方式（自动记忆） |
| `password` | `str` | — | 压缩密码（留空无密码，非空 ≥ 8 位，自动记忆） |
| `volume` | `str\|None` | `None` | 手动分卷大小，留空自动检测（自动记忆） |
| `custom_names` | `dict` | 31 条预置全关 | 扩展名 → 文件夹名映射，带勾选，未勾选的文件跳过（自动记忆） |
| `keep_files` | `bool` | `False` | 压缩后是否保留原始文件（自动记忆） |
| `double_compress` | `bool` | `True` | 是否二次打包为 `.zipp`（自动记忆） |
| `auto_close` | `bool` | `True` | 是否自动关闭 Bandizip 窗口（自动记忆） |

---

## 输出结构示例

```
E:\测试输出\
├── 文档\              # 自定义分类名
│   ├── 1.txt
│   ├── 2.txt
│   ├── 1-4.zip        # 分卷（未开启二次打包时）
│   ├── 5-8.zipp       # 二次打包（开启后）
│   └── ...
├── jpg\               # 未在 custom_names 中时使用扩展名
│   ├── 1.jpg
│   ├── 2.jpg
│   └── ...
└── pdf\
    └── ...
```

---

## 项目结构

```
SortZip/
├── SortZip.py        # 核心逻辑（分类/重命名/压缩）
├── main.py           # PySide6 图形界面
├── dist/
│   └── SortZip.exe   # 打包好的可执行文件
├── README.md
└── CHANGELOG.md
```

## 注意事项

- **本工具仅支持 Bandizip**，请确保已安装并加入 PATH
- 密码非空时必须 ≥ 8 位，否则运行时会提示
- 扩展名映射默认全部不勾选，请根据需求手动启用
- 首次运行建议先在小范围测试
- `keep_files = False` 时会删除原始文件，请谨慎配置
- `.zipp` 是脚本自用的二次打包后缀，非标准格式

---

## 许可

MIT
